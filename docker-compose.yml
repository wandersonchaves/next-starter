services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=nextstarter
      - POSTGRES_PASSWORD=nextstarter
      - POSTGRES_DB=nextstarter
    volumes:
      - nextstarter_pg_data:/bitnami/postgresql

volumes:
  nextstarter_pg_data:
